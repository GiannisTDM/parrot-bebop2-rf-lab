__pycache__/
*.pyc
.DS_Store

# Local/editor state
.idea/
.vscode/
*.swp

# Device dumps can contain serial numbers, paired-device credentials, GPS logs,
# and factory calibration. Keep them local unless they are deliberately scrubbed.
captures/
firmware/
*.telnet.log
bebop_capture_telnet.log

# Machine-specific capture helpers and generated measurements.
capture_*.sh
measurements/
*.tgz
*.tar.gz
*.plf

# Earlier device-specific prototypes superseded by parrot_rf_lab.sh.
tools/pes_live_busybox.sh
tools/pes_live_sc2_busybox.sh
